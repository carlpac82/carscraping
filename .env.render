# ============================================
# RENDER ENVIRONMENT VARIABLES
# ============================================
# Copiar estas variáveis para o Render Dashboard
# Environment → Add Environment Variable
# ============================================

# === AUTENTICAÇÃO ===
# Passwords dos utilizadores (definir valores seguros)
ADMIN_PASSWORD=your_secure_password_here
CARLPAC82_PASSWORD=your_secure_password_here
DPRUDENTE_PASSWORD=your_secure_password_here

# Secret key (deixar o Render gerar automaticamente)
# SECRET_KEY=auto_generated_by_render

# === DATABASE ===
# PostgreSQL URL (copiar do PostgreSQL Database no Render)
# DATABASE_URL=postgresql://user:password@host:port/database

# === SCRAPING ===
# Modo de scraping (0=real, 1=test URLs)
TEST_MODE_LOCAL=0

# Usar Playwright (1=sim, 0=não)
USE_PLAYWRIGHT=1

# Desabilitar método requests (CarJet bloqueia IPs de data center)
DISABLE_CARJET_REQUESTS=1

# Serviço de scraping (deixar vazio para usar direct fetch)
# SCRAPER_SERVICE=scrapeops

# API Key do ScraperOps (se usar)
# SCRAPER_API_KEY=your_api_key_here

# País do proxy
# SCRAPER_COUNTRY=pt

# Forçar proxy para Carjet (0=não usar proxy)
# FORCE_PROXY_FOR_CARJET=0

# === AJUSTES DE PREÇO ===
# Ajuste percentual dos preços do CarJet
CARJET_PRICE_ADJUSTMENT_PCT=3.12

# Offset em euros
CARJET_PRICE_OFFSET_EUR=0

# === GMAIL OAUTH (Opcional) ===
# Se usar notificações por email
# GMAIL_CLIENT_ID=your_client_id
# GMAIL_CLIENT_SECRET=your_client_secret
# OAUTH_REDIRECT_URI=https://carrental-api-5f8q.onrender.com/api/oauth/gmail/callback

# === SCRAPERAPI (Opcional) ===
# Se usar ScraperAPI em vez de ScraperOps
# SCRAPERAPI_KEY=your_scraperapi_key

# === DESENVOLVIMENTO (NÃO USAR NO RENDER) ===
# DEV_NO_AUTH=0  # NUNCA definir no Render!

# ============================================
# INSTRUÇÕES:
# ============================================
# 1. Aceder ao Render Dashboard
# 2. Selecionar o serviço "carrental_api"
# 3. Ir para "Environment"
# 4. Adicionar cada variável acima (exceto as comentadas)
# 5. Para SECRET_KEY: usar "Generate Value"
# 6. Para DATABASE_URL: copiar do PostgreSQL Database
# 7. Save Changes
# ============================================
